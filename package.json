{
  "name": "airc-tmux-remote",
  "version": "1.4.2",
  "private": true,
  "description": "Private tmux remote display and input server for Android.",
  "license": "UNLICENSED",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js --no-ngrok --host 0.0.0.0",
    "check": "node scripts/check.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  }
}
